apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: whoami-prod
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Daniel-Makhoba-Emmanuel/GitOps-Repo.git
    path: platform-gitops/apps/whoami/chart
    targetRevision: 'main'
    helm:
      valueFiles:
        - ../values/prod.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
